import { Meta } from "@storybook/addon-docs/blocks";
import { ThemeSync } from "./ThemeSync";

<Meta title="Design system/Foundations/Typography" />

# Typography

Four families, each with one job. Every sample below renders in
`var(--font-*)` — the live values from [`typography.css`](./typography.css) —
not a duplicated font-family string.

<ThemeSync />

export const Specimen = ({ name, cssVar, weights, sample, size = 24 }) => (
  <div
    style={{
      border: "1px solid #d8d5cb",
      borderRadius: 12,
      padding: "20px 24px",
      marginBottom: 16,
      display: "flex",
      flexDirection: "column",
      gap: 10,
    }}
  >
    <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline" }}>
      <span style={{ fontFamily: "var(--font-sans)", fontWeight: 600, fontSize: 15, color: "#222" }}>{name}</span>
      <span
        style={{
          fontFamily: "var(--font-mono)",
          fontSize: 10,
          textTransform: "uppercase",
          letterSpacing: "0.08em",
          color: "#767267",
        }}
      >
        {weights}
      </span>
    </div>
    <p style={{ margin: 0, fontFamily: `var(${cssVar})`, fontSize: size, lineHeight: 1.3, color: "#222" }}>{sample}</p>
  </div>
);

<Specimen
  name="Geist"
  cssVar="--font-sans"
  weights="400 · 500 · 600"
  sample="Some bright spots, some edges."
  size={30}
/>

<Specimen
  name="JetBrains Mono"
  cssVar="--font-mono"
  weights="400 · 500"
  sample="YOUR CHECK-IN · 3.5 · PLT-412"
  size={16}
/>

<Specimen
  name="Caveat"
  cssVar="--font-hand"
  weights="500 · 600"
  sample={`"Cached the org query — instant win."`}
  size={22}
/>

<Specimen
  name="Instrument Serif — reserved, not yet used"
  cssVar="--font-serif"
  weights="italic 0/1"
  sample="A quiet sprint."
  size={26}
/>

## Type scale, as used

export const scale = [
  ["34px", "Semibold, −0.02em", "Geist", "Hero sprint headline"],
  ["28px", "Semibold, tabular", "Geist", "Stat readout value"],
  ["19px", "Medium", "Caveat", "Sticky-note body"],
  ["16px", "Regular", "Geist", "Rating-row question, body"],
  ["14px", "Medium / Semibold", "Geist", "Ticket title / sidebar identity name"],
  ["13px", "Regular, tabular", "JetBrains Mono", "Dimension-bar value"],
  ["12px", "Regular, +0.14em", "JetBrains Mono", "Eyebrow, dimension label"],
  ["11px", "Regular, +0.04em", "JetBrains Mono", "Ticket ID, tooltip"],
  ["9px", "Regular, +0.1em", "JetBrains Mono", "Admin badge chip"],
];

<table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13, color: "#222" }}>
  <thead>
    <tr style={{ textAlign: "left", borderBottom: "1px solid #d8d5cb" }}>
      <th style={{ padding: "6px 8px" }}>Size</th>
      <th style={{ padding: "6px 8px" }}>Weight</th>
      <th style={{ padding: "6px 8px" }}>Family</th>
      <th style={{ padding: "6px 8px" }}>Used for</th>
    </tr>
  </thead>
  <tbody>
    {scale.map(([size, weight, family, usedFor]) => (
      <tr key={size} style={{ borderBottom: "1px solid #eee" }}>
        <td style={{ padding: "6px 8px", fontFamily: "var(--font-mono)" }}>{size}</td>
        <td style={{ padding: "6px 8px" }}>{weight}</td>
        <td style={{ padding: "6px 8px" }}>{family}</td>
        <td style={{ padding: "6px 8px", color: "#767267" }}>{usedFor}</td>
      </tr>
    ))}
  </tbody>
</table>
